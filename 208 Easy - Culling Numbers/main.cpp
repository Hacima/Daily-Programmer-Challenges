#include <iostream>
using namespace std;

int main() {
	string numbers = "1 1 2 2 3 3 4 4";
	string results;
	
	for (int i = 0; i < numbers.length(); i += 1) {
		/*iterate through the numbers string, adding unique numbers to the results string
		comapre each number to all the numbers in the results string and ignore all matches
		find a way to only include characters between blank spaces...*/
	}
	
}
